# Environment Variables — Copy this to .env and fill in values

# Server
PORT=4000
NODE_ENV=development

# Admin credentials (used for /api/admin/login)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=samfitness2026
JWT_SECRET=samfitness_super_secret_jwt_key_change_in_production

# Optional: Real SMTP (leave blank to use Ethereal auto test account)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
NOTIFICATION_EMAIL=admin@samfitness.com

# Frontend origin (for CORS)
FRONTEND_URL=http://localhost:5173
